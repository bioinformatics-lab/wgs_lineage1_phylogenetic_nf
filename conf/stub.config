params {
reference ="${baseDir}/data/mock_data/reference.gbk"
reads ="${baseDir}/data/mock_data/*_R{1,2}.fastq.gz"
}

process {
    withName:
    ".*" {
        container = "gliderlabs/alpine"
        cpus = 2
        memory = "2 GB"
    }
}
